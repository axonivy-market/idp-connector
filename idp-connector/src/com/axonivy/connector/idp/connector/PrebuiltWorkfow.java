package com.axonivy.connector.idp.connector;

/**
 * Enum class for pre-built workflow: generic-splitting, invoice-extraction 
 *
 */
public enum PrebuiltWorkfow {
	generic_splitting,
	invoice_extraction
	;
}
